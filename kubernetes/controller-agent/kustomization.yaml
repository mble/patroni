apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml

configMapGenerator:
  - name: patroni-split-agent
    files:
      - agent.yml
  - name: patroni-split-controller
    files:
      - controller.yml

generatorOptions:
  disableNameSuffixHash: true
